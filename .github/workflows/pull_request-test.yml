name: Invoke
on:
  pull_request:
    types: [opened, synchronize, closed]
  workflow_dispatch:
jobs:
  output-test:
    runs-on: ubuntu-20.04
    steps:
      - uses: mkubasz/one-action@master
        with:
          my_selector: ".pull_request"
